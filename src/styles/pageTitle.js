import styled from 'styled-components'

const PageTitle = styled.header`
  max-width: 1040px;
  text-align: center;
  margin: 0px auto;
  padding: 6vw 3vw 3vw;

  h1 {
    font-weight: 700;
    color: rgb(11, 12, 14);
    font-size: 2.8rem;
  }
`

export default PageTitle
